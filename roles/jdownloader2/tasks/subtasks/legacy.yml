##########################################################################
# Title:            Sandbox: JDownloader2 | Legacy                       #
# Author(s):        kowalski, desimaniac                                 #
# URL:              https://github.com/saltyorg/Sandbox                  #
# --                                                                     #
##########################################################################
#                   GNU General Public License v3.0                      #
##########################################################################
---
- name: Legacy | Check if '{{ server_appdata_path }}/myjdownloader' exists
  ansible.builtin.stat:
    path: "{{ server_appdata_path }}/myjdownloader"
  register: opt_myjdownloader

- name: Legacy | Migration Block
  when: (opt_myjdownloader.stat.exists)
  block:
    - name: Legacy | Stop and remove myjdownloader container
      community.docker.docker_container:
        name: myjdownloader
        state: absent

    - name: Legacy | Move opt folder 'myjdownloader' to 'jdownloader2'
      ansible.builtin.shell: |
        mv {{ server_appdata_path }}/myjdownloader "{{ lookup('role_var', '_paths_location', role='jdownloader2') }}"

    - name: Legacy | Set permissions on 'jdownloader2'
      ansible.builtin.file:
        path: "{{ lookup('role_var', '_paths_location', role='jdownloader2') }}"
        state: directory
        recurse: true
        owner: "{{ user.name }}"
        group: "{{ user.name }}"
