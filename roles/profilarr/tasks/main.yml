#########################################################################
# Title:            Sandbox: Profilarr | Deployment Tasks               #
# Author(s):        keldian                                             #
# URL:              https://github.com/saltyorg/Sandbox                 #
# --                                                                    #
#########################################################################
#                   GNU General Public License v3.0                     #
#########################################################################
---
#########################################################################
# Check for existing Profilarr container and migrate to v2              #
#########################################################################

- name: Check for existing Profilarr container
  community.docker.docker_container_info:
    name: profilarr
  register: profilarr_container
  failed_when: false

- name: Set existing Profilarr facts
  ansible.builtin.set_fact:
    profilarr_container_exists: "{{ profilarr_container.exists | default(false) }}"
    profilarr_existing_image: "{{ profilarr_container.container.Config.Image | default('') }}"
    profilarr_existing_version: "{{ profilarr_container.container.Config.Labels['org.opencontainers.image.version'] | default('') }}"
    profilarr_existing_description: "{{ profilarr_container.container.Config.Labels['org.opencontainers.image.description'] | default('') }}"

- name: Detect existing Profilarr v1
  ansible.builtin.set_fact:
    profilarr_existing_is_v1: >-
      {{
        profilarr_container_exists
        and (
          profilarr_existing_image is search('santiagosayshey/profilarr')
          or profilarr_existing_version == 'beta'
          or profilarr_existing_description == 'Profilarr - Profile manager for *arr apps'
        )
      }}

- name: Confirm Profilarr v1 replacement
  ansible.builtin.pause:
    prompt: |
      Existing Profilarr v1 container detected.

      Profilarr v2 requires a fresh setup.
      There is no migration path from v1 to v2.

      If you continue, this playbook will:
        1. Stop and remove the existing profilarr container
        2. Back up /opt/profilarr to a timestamped directory
        3. Continue installing Profilarr v2

      Type 'yes' to continue, or anything else to abort
  register: profilarr_v1_confirm
  when: profilarr_existing_is_v1

- name: Abort Profilarr v2 install if user declines v1 replacement
  ansible.builtin.fail:
    msg: "Profilarr v2 install aborted by user."
  when:
    - profilarr_existing_is_v1
    - profilarr_v1_confirm.user_input | lower != 'yes'

- name: Migrate existing Profilarr v1 aside
  when:
    - profilarr_existing_is_v1
    - profilarr_v1_confirm.user_input | lower == 'yes'
  block:

    - name: Generate Profilarr v1 backup path
      ansible.builtin.set_fact:
        profilarr_v1_backup_path: >-
          /opt/profilarr_v1_{{ ansible_facts['date_time']['iso8601_basic_short'] }}

    - name: Stop and remove existing Profilarr v1 container
      community.docker.docker_container:
        name: profilarr
        state: absent

    - name: Check whether Profilarr config directory exists
      ansible.builtin.stat:
        path: /opt/profilarr
      register: profilarr_v1_config_dir

    - name: Back up existing Profilarr v1 config directory
      ansible.builtin.command:
        cmd: mv /opt/profilarr {{ profilarr_v1_backup_path }}
      when: profilarr_v1_config_dir.stat.exists

    - name: Show backup location
      ansible.builtin.debug:
        msg: "Existing Profilarr v1 config backed up to {{ profilarr_v1_backup_path }}"

#########################################################################

- name: Add DNS record
  ansible.builtin.include_tasks: "{{ resources_tasks_path }}/dns/tasker.yml"
  vars:
    dns_record: "{{ lookup('role_var', '_dns_record') }}"
    dns_zone: "{{ lookup('role_var', '_dns_zone') }}"
    dns_proxy: "{{ lookup('role_var', '_dns_proxy') }}"

- name: Remove existing Docker container
  ansible.builtin.include_tasks: "{{ resources_tasks_path }}/docker/remove_docker_container.yml"

- name: Create directories
  ansible.builtin.include_tasks: "{{ resources_tasks_path }}/directories/create_directories.yml"

- name: Create Docker container
  ansible.builtin.include_tasks: "{{ resources_tasks_path }}/docker/create_docker_container.yml"
