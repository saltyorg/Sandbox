##########################################################################
# Title:         Sandbox: Semaphoreui | Default Variables                     #
# Author(s):     Barrow1990                                                   #
# URL:           https://github.com/saltyorg/Sandbox                     #
# --                                                                     #
##########################################################################
#                   GNU General Public License v3.0                      #
##########################################################################
---
################################
# Basics
################################

semaphoreui_name: semaphoreui

################################
# Settings
################################

semaphoreui_postgres_name: "{{ semaphoreui_name }}-postgres"
semaphoreui_postgres_role_docker_image_tag: "15"
semaphoreui_postgres_role_docker_image_repo: "postgres"
semaphoreui_postgres_role_docker_env_db: "semaphoreui"
semaphoreui_postgres_role_docker_env_user: "semaphoreui"
semaphoreui_postgres_role_docker_env_password: "semaphoreui"
semaphoreui_postgres_role_paths_folder: "{{ semaphoreui_name }}"
semaphoreui_postgres_role_paths_location: "{{ server_appdata_path }}/{{ postgres_role_paths_folder }}/postgres"

################################
# Paths
################################

semaphoreui_role_paths_folder: "{{ semaphoreui_name }}"
semaphoreui_role_paths_location: "{{ server_appdata_path }}/{{ semaphoreui_role_paths_folder }}"
semaphoreui_role_paths_folders_list:
  - "{{ server_appdata_path }}/{{ semaphoreui_role_paths_folder }}/playbooks"

################################
# Web
################################

semaphoreui_role_web_subdomain: "{{ semaphoreui_name }}"
semaphoreui_role_web_domain: "{{ user.domain }}"
semaphoreui_role_web_port: "3000"
semaphoreui_role_web_url: "{{ 'https://' + (semaphoreui_role_web_subdomain + '.' + semaphoreui_role_web_domain
                      if (semaphoreui_role_web_subdomain | length > 0)
                      else semaphoreui_role_web_domain) }}"

################################
# DNS
################################

semaphoreui_role_dns_record: "{{ semaphoreui_role_web_subdomain }}"
semaphoreui_role_dns_zone: "{{ semaphoreui_role_web_domain }}"
semaphoreui_role_dns_proxy: "{{ dns.proxied }}"

################################
# Traefik
################################

semaphoreui_role_traefik_sso_middleware: "{{ traefik_default_sso_middleware }}"
semaphoreui_role_traefik_middleware_default: "{{ traefik_default_middleware }}"
semaphoreui_role_traefik_middleware_custom: ""
semaphoreui_role_traefik_certresolver: "{{ traefik_default_certresolver }}"
semaphoreui_role_traefik_enabled: true
semaphoreui_role_traefik_api_enabled: true
semaphoreui_role_traefik_api_endpoint: "PathPrefix(`/api`)"

################################
# Docker
################################

# Container
semaphoreui_role_docker_container: "{{ semaphoreui_name }}"

# Image
semaphoreui_role_docker_image_pull: true
semaphoreui_role_docker_image_tag: "latest"
semaphoreui_role_docker_image: "semaphoreui/semaphore:{{ semaphoreui_role_docker_image_tag }}"

# Envs
semaphoreui_role_docker_envs_default:
  - SEMAPHORE_DB_USER: semaphoreui
  - SEMAPHORE_DB_PASS: semaphoreui
  - SEMAPHORE_DB_HOST: "{{ semaphoreui_postgres_name }}" # for postgres, change to: postgres
  - SEMAPHORE_DB_PORT: "5432" # change to 5432 for postgres
  - SEMAPHORE_DB_DIALECT: postgres # for postgres, change to: postgres
  - SEMAPHORE_DB: semaphoreui
  - SEMAPHORE_PLAYBOOK_PATH: "{{ semaphoreui_role_paths_location }}/playbooks"
  - SEMAPHORE_ADMIN_PASSWORD: "{{ user.pass }}"
  - SEMAPHORE_ADMIN_NAME: "{{ user.name }}"
  - SEMAPHORE_ADMIN_EMAIL: "{{ user.email }}"
  - SEMAPHORE_ADMIN: "{{ user.name }}"
  - SEMAPHORE_ACCESS_KEY_ENCRYPTION: "{{ semaphoreui_saltbox_facts.facts.secret_key }}"
  - TZ: "{{ timezone }}"
semaphoreui_role_docker_envs_custom: {}
semaphoreui_role_docker_envs: "{{ semaphoreui_role_docker_envs_default
                        | combine(semaphoreui_role_docker_envs_custom) }}"

# Hostname
semaphoreui_role_docker_hostname: "{{ semaphoreui_name }}"

# Networks
semaphoreui_role_docker_networks_alias: "{{ semaphoreui_name }}"
semaphoreui_role_docker_networks_default: []
semaphoreui_role_docker_networks_custom: []
semaphoreui_role_docker_networks: "{{ docker_networks_common
                            + semaphoreui_role_docker_networks_default
                            + semaphoreui_role_docker_networks_custom }}"

# Restart Policy
semaphoreui_role_docker_restart_policy: unless-stopped

# State
semaphoreui_role_docker_state: started

# Dependencies
semaphoreui_depends_on: "{{ semaphoreui_postgres_name }}"
semaphoreui_depends_on_delay: "0"
semaphoreui_depends_on_healthchecks: "false"
